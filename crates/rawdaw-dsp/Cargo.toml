[package]
name = "rawdaw-dsp"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
# Pure DSP primitives — no engine or model deps. The crate is intentionally
# leaf-level so every synth / fx crate can depend on it without inheriting
# domain knowledge that belongs higher up.
